{
  "name": "@tracemap/typescript",
  "version": "0.1.0",
  "private": true,
  "description": "TraceMap TypeScript scanner",
  "bin": {
    "tracemap-ts": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "check": "npm run build && npm run test"
  },
  "dependencies": {
    "sql.js": "^1.12.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/sql.js": "^1.4.9",
    "vitest": "^4.0.15"
  }
}
