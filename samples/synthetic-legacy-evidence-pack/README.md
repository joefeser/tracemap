# Synthetic Legacy Evidence Pack Fixture

This directory contains redacted, public-safe summary input for
`tracemap evidence-pack`. It is intentionally not a raw scan output.

Use it to validate evidence-pack generation without publishing local paths,
raw SQL, config values, snippets, remotes, or private sample names.
