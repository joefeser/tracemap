# Tasks

- [x] 1. Add the public demo walkthrough page.
  - Add `/demo/start-here/`.
  - Include `Public claim level: demo`.
  - Show the first-run workflow, command snippet, output inspection path, and
    static-evidence boundaries.

- [x] 2. Add discovery links.
  - Link the walkthrough from `/demo/`.
  - Link the walkthrough from the homepage source-of-truth section.
  - Add `/demo/start-here/` to generated sitemap metadata.

- [x] 3. Add spec-local implementation state.
  - Record branch, scope, validation, boundaries, and follow-ups.

- [x] 4. Validate.
  - Run `npm test` from `site/`.
  - Run `npm run validate` from `site/`.
  - Run a desktop and mobile browser sanity check.

