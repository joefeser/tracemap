# site-tracemap-tools-public-demo-result implementation state

## Status

Implemented.

## Public claim level

demo

## Branch

`codex/site-public-demo-result`

## Scope

This is a website/content phase only. It adds `/demo/result/` to explain the
current public demo result shape from the first public demo workflow slice.

The page is based on:

- `scripts/demo-public.sh`
- `scripts/demo-public-assert.mjs`
- `.kiro/specs/public-demo-workflow/implementation-state.md`
- `README.md`
- `docs/VALIDATION.md`

## Current demo boundary

- Available: toolchain checks, .NET/TypeScript build, and four checked-in sample
  scans.
- Default Python status: `not_requested` unless requested.
- JVM status: environment-dependent; unavailable when Java 21 is absent unless
  required.
- Deferred in this first slice: combine/dependency report, paths/reverse,
  portfolio, diff, impact, and release-review.

## Claim boundaries

- Do not present deferred demo sections as available demo output.
- Do not say TraceMap proves runtime behavior, production traffic, endpoint
  performance, deployment state, incident cause, or release safety.
- Do not say this is AI analysis.
- Keep local-only generated artifacts out of public copy: raw SQLite, facts,
  manifests, logs, raw source snippets, raw SQL/config values, secrets, local
  absolute paths, and raw repository remotes.

## Validation

- `npm test`
- `npm run validate`
- `git diff --check`
- Browser sanity for desktop and mobile `/demo/result/`
- Browser sanity for linked doorway pages

## Follow-ups

- Replace representative summary excerpts with checked-in public-safe generated
  excerpts only after the demo produces stable public artifacts intended for the
  site.
- Add result screenshots after public-safe generated artifacts are checked in or
  generated by the site build.
- Upgrade deferred rows only after follow-up demo assertions land.

