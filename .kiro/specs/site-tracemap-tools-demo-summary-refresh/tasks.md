# Tasks

- [ ] 1. Add the public-safe demo summary fixture contract.
  - Define the committed fixture path under `site/src/`.
  - Include version, source summary metadata, public claim level, sections,
    names, statuses, classifications, evidence tiers, rule IDs, coverage
    labels, counts, reasons, and public-safe artifact paths.
  - Map from real `demo-summary.json` fields: `name`, `status`,
    `classification`, `evidenceTier`, `ruleIds`, `reportCoverage`,
    `artifactPaths`, `counts`, and `reason`.
  - Use a checked known-name mapping table if the fixture adds stable `id`
    values; fail on unknown names.
  - Document that the refresh mechanism remains hidden even when the data feeds
    demo-level pages.

- [ ] 2. Add an explicit local refresh command.
  - Read only an explicit output root generated by `./scripts/demo-public.sh`.
  - Parse `demo-summary.json` and, if needed, bounded public-safe report JSON.
  - Reject missing rule IDs, missing evidence tiers, unsupported summary
    versions, unsafe paths, unknown raw artifact families, unknown section
    names, and missing reasons for deferred, unavailable, or failed sections.
  - Reject any `portfolio-manifest.json` whose `indexPath` values are not
    approved relative paths before using manifest-derived fields.
  - Write deterministic fixture JSON with stable ordering and a trailing
    newline.

- [ ] 3. Add fixture validation.
  - Validate fixture schema and allowed field values.
  - Reject local absolute paths, raw repository remotes, path traversal,
    `file://` URLs, secrets, source snippets, raw SQL, config values, raw scan
    artifacts, SQLite files, generated scan directories, and analyzer logs.
  - Wire the validation into the existing cheap site validation path.
  - Add a golden round-trip test from sample `demo-summary.json` to fixture
    JSON.
  - Add an unsupported `demo-summary.json` `version` rejection test.
  - Add field-mapping tests for `reportCoverage` and `artifactPaths`.
  - Add focused rejection tests for each unsafe path and value class.
  - Add tests that refresh input selection ignores files under `scans/`,
    `combined/`, and `logs/`.
  - Add a duplicate section-name rejection test.
  - Add a build/exclusion test in `site/scripts/build.test.mjs` that places a
    fixture under a `_data/` subdirectory and proves it does not appear in
    `site/dist/`.

- [ ] 4. Update affected pages to avoid manual drift.
  - Validate `/demo/result/` against the fixture.
  - Validate `/demo/proof-upgrades/` against the fixture.
  - Validate `/demo/proof-assets/`, including hard-coded counts, against the
    fixture.
  - Validate `/packets/` against the fixture.
  - Validate `/manager-packet/` against the fixture.
  - Validate `/capabilities/` against the fixture.
  - Make validation extract hard-coded numeric and label values from HTML
    source and compare them to the fixture so page copy cannot drift silently.

- [ ] 5. Preserve static-site and public-safety boundaries.
  - Keep the site build static: no backend, runtime service, or client-side
    local artifact fetch.
  - Keep raw generated output outside `site/dist/` and outside committed site
    data.
  - Keep public copy tied to rule IDs, evidence tiers, coverage labels, counts,
    limitations, and public-safe generated artifact names.
  - Do not claim runtime behavior, production traffic, deployment state,
    endpoint performance, production dependency understanding, P1 root cause,
    release safety, or AI impact analysis.

- [ ] 6. Validate the implementation.
  - Run `git diff --check`.
  - Run `npm test` from `site/`.
  - Run `npm run validate` from `site/`.
  - Run browser sanity checks only if page layout or interaction changes.
  - Update this spec's implementation-state note with branch, validation,
    review-loop results, oddities, and follow-ups.
