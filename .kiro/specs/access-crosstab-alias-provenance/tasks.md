# Tasks

- [x] Record the crosstab `[1]`/`W0` provenance gap and its non-equivalence boundary.
- [x] Extend Access output projections with alias and source-expression provenance.
- [x] Preserve pivot-expression source candidates separately from aggregate sources.
- [x] Emit the provenance through existing query-output facts.
- [x] Add deterministic projector, reader, and fact-builder regression coverage.
- [x] Update the `legacy.access.query.v1` limitation text.
- [ ] Rerun the private Windows corpus from one immutable database copy if exact
      QueryDef property reconciliation is still required.
