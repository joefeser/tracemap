# EF Core Mapping v0 Tasks

- [x] Audit existing EF facts, annotation mappings, combined surfaces, and the
      database design-review seam.
- [x] Define the bounded v0 extraction, gap, composition, and non-claim
      boundary.
- [x] Enrich semantic `DbSet`, table, and column mapping evidence with bounded
      entity/member/schema identity.
- [x] Add constant fluent `ToTable` and `HasColumnName` mapping extraction.
- [x] Add dynamic and assembly-driven model-configuration gaps.
- [x] Compose EF mappings and gaps into database design review.
- [x] Catalog the updated rule emissions and limitations.
- [x] Add focused extractor and packet regression tests.
- [x] Run focused/full validation, smoke checks, private-path checking, and
      `git diff --check`.
- [x] Update implementation state with exact validation and deferred work.
- [x] Commit, push, open a ready PR to `dev`, and follow ACK.
