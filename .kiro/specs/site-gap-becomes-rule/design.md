# Site Gap Becomes Rule Design

The article uses the generated blog shell and one focused dist validator. The validator checks the concept claim level and rejects completeness, automatic repair, private validation, runtime, production, release, safety, source, SQL, credential, private-path, and AI-analysis overclaims.

