# Site TraceMap Tools Public Visual Proof Assets Tasks

Public claim level: demo

- [x] Create `/demo/proof-assets/` using existing static site layout patterns.
- [x] Add public-safe visual proof examples for report summary, paths/reverse, diff/impact, and release-review outputs.
- [x] Keep every visual tied to rule/status framing, evidence tier or coverage language, and visible limitations.
- [x] State that visuals are orientation only and that generated reports, facts, indexes, rule IDs, coverage labels, counts, and limitations remain the source of truth.
- [x] Add explicit artifact safety boundaries that keep raw snippets, SQL, config, local paths, facts, SQLite, generated scan directories, combined SQLite files, and analyzer logs local-only.
- [x] Add `/demo/proof-assets/` to `site/src/_site/pages.json`.
- [x] Link `/demo/` to `/demo/proof-assets/`.
- [x] Link `/demo/proof-upgrades/` to `/demo/proof-assets/`.
- [x] Link `/demo/result/` to `/demo/proof-assets/`.
- [x] Link `/packets/` to `/demo/proof-assets/`.
- [x] Link `/roadmap/` to `/demo/proof-assets/` with bounded demo-level wording.
- [x] Run Kiro/Sonnet spec review before implementation and patch Medium+ spec findings.
- [x] Run `git diff --check`.
- [x] Run `npm test` from `site/`.
- [x] Run `npm run validate` from `site/`.
- [x] Run desktop and mobile browser sanity checks for layout and overflow.
- [x] Update this task list and `implementation-state.md` with validation results, scope decisions, oddities, and follow-ups.
