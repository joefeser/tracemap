# Site TraceMap Tools Public Visual Proof Assets Tasks

Public claim level: demo

- [ ] Create `/demo/proof-assets/` using existing static site layout patterns.
- [ ] Add public-safe visual proof examples for report summary, paths/reverse, diff/impact, and release-review outputs.
- [ ] Keep every visual tied to rule/status framing, evidence tier or coverage language, and visible limitations.
- [ ] State that visuals are orientation only and that generated reports, facts, indexes, rule IDs, coverage labels, counts, and limitations remain the source of truth.
- [ ] Add explicit artifact safety boundaries that keep raw snippets, SQL, config, local paths, facts, SQLite, generated scan directories, combined SQLite files, and analyzer logs local-only.
- [ ] Add `/demo/proof-assets/` to `site/src/_site/pages.json`.
- [ ] Link `/demo/` to `/demo/proof-assets/`.
- [ ] Link `/demo/proof-upgrades/` to `/demo/proof-assets/`.
- [ ] Link `/demo/result/` to `/demo/proof-assets/`.
- [ ] Link `/packets/` to `/demo/proof-assets/`.
- [ ] Link `/roadmap/` to `/demo/proof-assets/` with bounded demo-level wording.
- [x] Run Kiro/Sonnet spec review before implementation and patch Medium+ spec findings.
- [ ] Run `git diff --check`.
- [ ] Run `npm test` from `site/`.
- [ ] Run `npm run validate` from `site/`.
- [ ] Run desktop and mobile browser sanity checks for layout and overflow.
- [ ] Update this task list and `implementation-state.md` with validation results, scope decisions, oddities, and follow-ups.
