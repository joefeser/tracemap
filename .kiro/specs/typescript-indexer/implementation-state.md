# TypeScript Indexer Implementation State

Status: implemented-mvp

## Shipped Scope

- Added `src/typescript` and `tracemap-ts scan`.
- Emits deterministic TypeScript facts, manifests, SQLite indexes, reports, package/config evidence, semantic and syntax fallback facts, route/client/SQL integration facts, and reducer-compatible evidence.

## Post-MVP Backlog

JavaScript support, workspace discovery, root CLI dispatch, broader frameworks, field/property aliasing, parameter-forward edges, and multi-index workflows are backlog items. They are plain bullets in `tasks.md`, not active unchecked tasks.
