# Database Design-Review Packet Tasks

- [x] Audit existing PostgreSQL schema, SQL surface, and path-report read models.
- [x] Record the combined-index, read-side-only composition boundary.
- [x] Add packet and provenance DTOs with deterministic identities and ordering.
- [x] Add combined fact/schema/snapshot projection with declarations separated
      from operations.
- [x] Add exact source-scoped query/table correlation and unlinked-query gaps.
- [x] Compose only existing bounded endpoint/legacy-root paths to SQL terminals.
- [x] Add deterministic Markdown/JSON output and bounded truncation gaps.
- [x] Add `database-design-review` CLI parsing, help, and output behavior.
- [x] Catalog packet/gap rules and document limitations before emitting them.
- [x] Add focused grouping, route, provenance, truncation, determinism, and leak
      regression tests.
- [x] Run focused/full validation, CLI smoke, private-path checking, and
      `git diff --check`.
- [x] Update this checklist and implementation state with exact validation.
- [x] Commit, push, open a ready PR to `dev`, and follow ACK.
