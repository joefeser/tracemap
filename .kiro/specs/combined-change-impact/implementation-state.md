# Combined Change Impact Implementation State

Status: implemented

## Shipped Scope

- Added `tracemap impact` for deterministic combined before/after static change context.
- Converts combined diff evidence into impact items with rule IDs, evidence tiers, file spans, supporting IDs, classifications, coverage caveats, and limitations.
- Supports selectors, caps, deterministic Markdown/JSON, safe rendering, and optional path context boundaries.

## Follow-Ups

- Deeper path-context polish should use a new focused spec or a new clearly scoped follow-up branch.
- Do not re-open unchecked task boxes in this spec for general backlog.
