# Site C# Extraction Truth Design

The existing article shell generates the route and sitemap entry. The article
uses checked-in fixture-matrix and rule-catalog contracts without reproducing
source fixtures or analyzer output. A focused validator guards the generated
route, metadata/discovery contract, required evidence vocabulary, links, and
public-safety boundary.

Issue #617's static-dispatch articles are not yet available on `dev`; this
article names that ownership boundary without adding a broken route.
