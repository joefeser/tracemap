# Query Pattern Reporting Implementation State

Status: implemented

## Shipped Scope

- Added Python query-pattern report rendering for SQL-shape evidence.
- Includes deterministic display of operation, table/column identifiers, source kind, shape hash, evidence tier, spans, and limitations without raw SQL.

## Follow-Ups

- Cross-adapter report alignment is covered by `query-pattern-reporting-v2`.
