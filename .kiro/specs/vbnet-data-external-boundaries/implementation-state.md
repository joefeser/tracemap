# Implementation state

- Branch: `codex/issue-737-vbnet-data-boundaries`
- Base: `origin/dev` at `debf5a8fa5720775cb60337a9d94053e83689a4f`
- Issue: #737
- Current scope: compiler-backed Visual Basic data and external-boundary issue implemented; PR #749 review corrections are validated locally and pending push.
- Contract decision: reuse `SqlCommandDetected` / `database.sql.text.v1` and `DatabaseOperationCandidate` / `database.operation.call-pattern.v1` so existing public consumers can ingest VB evidence.
- Privacy decision: retain categorical command metadata and optional constant-value hashes; never retain raw SQL, procedure names, connection strings, or parameter values.
- Join decision: correlate only Roslyn-resolved receiver symbols within a containing method; spelling-only matches remain gaps.
- Implemented evidence: compiler-resolved DbCommand construction, CommandType assignment, parameter collection mutation, ExecuteReader/ExecuteScalar/ExecuteNonQuery families, and DbDataAdapter.Fill for DataSet/DataTable. Existing object/call facts retain DataSet, DataTable, reader, and supporting call-path evidence.
- Downstream decision: database, WCF, and ASMX mappings are supported Web Forms terminals using existing closed kinds. Data-adapter Fill retains the shared `sql-query` classification, while execution candidates remain `sql-persistence`. C# and VB extraction remain language-specific while projection contracts are shared. Shared Web Forms extractor is `legacy-webforms/0.8.5`.
- Safety: constant command strings retain hash and length only. Stored-procedure evidence is the categorical `StoredProcedure` enum assignment joined by compiler-resolved receiver identity. Raw SQL/procedure/parameter/connection values are not retained.
- Gap budget: unresolved name-only Fill/Execute candidates emit at most 50 site gaps plus one deterministic budget-exhaustion gap per document and never emit operation candidates.
- External evidence: shared HTTP facts cover compiler-resolved HttpClient, WebRequest, and WebClient calls; ConfigurationManager/My.Settings access reuses ConfigBinding; VB Global.System.IO display strings feed the shared batch/data-movement extractor. Hosts, raw URLs, config keys/values, and file arguments are not retained by these new facts.
- WebRequest decision: construction with a constant string or New Uri constant retains safe normalized-path/hash evidence; a later parameterless GetResponse remains an unknown-destination HTTP fact plus a gap because cross-statement receiver correlation is not yet established.
- Service decision: the VB semantic lane emits existing WCF/ASMX declaration facts only for recognized strong-named framework identities. WCF operations require exact interface implementation; ASMX operations require recognized framework SOAP/WebMethod attributes. Existing mappers compose review-tier mappings; unsupported methods retain Tier4 gaps and unsigned same-name lookalikes produce no semantic service facts. The public-key token is an identity discriminator, not an authenticity or product-trust claim.
- Downstream validation: a synthetic VB Web Forms handler reaches shared HTTP/config/file evidence through handler/call-path projection; the generated packet, docs/query recipes, and WITS-compatible corpus validator consume the same facts without a VB-only schema. Existing code-path/source view consumes the same retained spans and call facts.
- Remaining issue scope: none in the Kiro task list; changes remain unpushed for review.
- Review corrections: renamed and inherited WCF interface operations retain exact contract identity; ASMX operations require a compiler-proven WebService context and flow through the Web Forms service-mapping join; classic .NET Framework HTTP/configuration assembly names are admitted with exact metadata type checks; unresolved command constructions emit bounded gaps; same-name ADO.NET helper overloads are rejected; and adapter Fill retains the shared `sql-query` terminal kind. Producer-local external gaps remain explicit facts but intentionally do not convert a successful compiler/toolchain receipt into a build failure.
- Validation: full solution suite passes 1922/1922. Dedicated data/external-boundary tests pass 10/10 and cover the review regressions plus HttpClient/New Uri, WebRequest construction/unknown response destination, WebClient, ConfigurationManager section/indexers, System.IO projection, compiler-proven WCF/ASMX mappings, unsupported service gaps, unsigned same-name framework lookalikes, late binding, privacy, determinism, packet/docs/query-recipe/WITS corpus consumption. Shared batch tests pass with the VB display-string compatibility seam. The pinned `community-visual-basic` smoke at `20d2a51dfc9f342848ad134952ceaa8d79302559` completed twice after clean resets with byte-identical `facts.ndjson` (`8d218dcb73eec9fd4a77e72969719c4c1c64726f6427f1ed6302484353675e69`): 110,726 facts, 6,314 VB symbols, 928 semantic VB call edges, 517 object creations, 175 argument flows, 98 relationships, and 75,455 explicit gaps; coverage is `Level1SemanticAnalysisReduced` / `FailedOrPartial` as expected for the pinned legacy repository.
