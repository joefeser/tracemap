# Tasks

- [x] 1. Add the examples spec and implementation state.
  - Record scope, validation expectations, and follow-ups.

- [x] 2. Add example pages.
  - Add `/examples/`.
  - Add `/examples/scan-packet/`.
  - Add `/examples/contract-impact/`.

- [x] 3. Add use-case framing.
  - Add `/use-cases/`.
  - Keep use cases bounded to static evidence and coverage labels.

- [x] 4. Update navigation and metadata.
  - Add `Examples` to primary navigation.
  - Add pages to `sitemap.xml`.
  - Add source-of-truth links back to GitHub docs.

- [x] 5. Validate.
  - Run `npm run build`.
  - Run local HTTP smoke for new URLs.
  - Check desktop and mobile browser layout.
