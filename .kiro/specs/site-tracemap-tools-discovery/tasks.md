# Tasks

- [x] 1. Record the discovery slice scope and launch state.
  - [x] Add this `site-tracemap-tools-discovery` spec.
  - [x] Add or update implementation-state notes for the launch and discovery site
    slices.

- [x] 2. Add stable crawlable topic pages.
  - [x] Add `/evidence/`, `/outputs/`, `/workflows/`, `/validation/`,
    `/limitations/`, and `/demo/`.
  - [x] Keep page claims bounded and tied back to TraceMap source docs.

- [x] 3. Update homepage navigation.
  - [x] Replace section-only navigation with stable page URLs.
  - [x] Keep GitHub and source-of-truth links visible.

- [x] 4. Update discovery metadata.
  - [x] Add all stable pages to `sitemap.xml`.
  - [x] Add a static 404 page.
  - [x] Keep `robots.txt` crawl-friendly.

- [x] 5. Update deployment documentation.
  - [x] Document Amplify app root, build command, output directory, and validation
    URLs in `site/README.md`.

- [x] 6. Validate.
  - [x] Run `npm run build`.
  - [x] Run a local preview smoke for homepage and new pages.
  - [x] Check desktop and mobile overflow with a browser.
