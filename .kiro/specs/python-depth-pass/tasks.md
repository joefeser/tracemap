# Python Depth Pass Tasks

- [x] Add compact spec for the Python depth pass.
- [x] Resolve import aliases in Python AST integration extractors.
- [x] Compose FastAPI router prefixes across static `include_router` calls.
- [x] Emit direct `FieldAlias` facts for `self.<field> = <name>` assignments.
- [x] Populate Python `field_aliases` rows.
- [x] Derive Python `parameter_forward_edges` rows from direct `ArgumentPassed` facts.
- [x] Add tests for aliases, composed routes, field aliases, and forwarding rows.
- [x] Run Python tests, .NET build/test, local smoke, private-path guard, and diff checks.
