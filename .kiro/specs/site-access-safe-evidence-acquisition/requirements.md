# Site Access Safe Evidence Acquisition Requirements

## Goal

Publish issue #620 as a public-safe article that explains how TraceMap obtains
bounded Microsoft Access static evidence without implying execution,
reconstruction, complete coverage, or operational approval.

## Requirements

1. Publish `/blog/reverse-engineering-access-without-running-it/` through the
   existing article registry and layout.
2. Explain the verified generic-name disposable snapshot and distinguish
   repository provenance from `local-file-snapshot` provenance.
3. Separate ordinary scanning, protected form/report design acquisition, and
   the optional protected VBA lane.
4. Describe startup suppression, invisible force-disabled automation, canaries,
   hashes, timeouts, process checks, cleanup, and fail-closed behavior.
5. Name the inventory, design-input, and coverage-gap rule IDs with evidence
   tiers and limitations.
6. Link evidence, gaps, static-versus-runtime, manager proof paths,
   capabilities, limitations, and change review.
7. Add discovery, sitemap, claim, link, and private-material regressions.
8. Keep the public claim level at `demo` and preserve issue #620's non-claims.
