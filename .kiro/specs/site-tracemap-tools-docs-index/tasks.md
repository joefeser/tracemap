# Tasks

- [x] 1. Add a public docs index page.
  - Add `/docs/` with links to PRD, validation, acceptance, language adapter
    contract, decisions, and next execution report.
  - Keep the repository as the source of truth.
  - Preserve deterministic static-analysis boundaries.

- [x] 2. Add discovery links.
  - Link the docs index from the homepage source-of-truth section.
  - Link the docs index from the validation page footer.
  - Add `/docs/` to generated sitemap metadata.

- [x] 3. Add spec-local implementation state.
  - Record the branch, scope, validation, and follow-ups.

- [x] 4. Validate.
  - Run `npm test` from `site/`.
  - Run `npm run validate` from `site/`.
