# Site Reverse Impact and Static Dispatch Design

The two articles reuse the existing generated blog layout. One focused dist validator checks both routes as a connected story: registry cards, discovery entries, sitemap URLs, reciprocal links, required evidence vocabulary, public claim boundaries, and private or executable material. Generated directories remain build-owned.

