# Tasks: Web Forms agent evidence handoff

- [x] 1. Define the private handoff schema, evidence boundary, and product non-claims.
- [x] 2. Emit deterministic private per-case handoffs and link them from collapsed HTML sections.
- [x] 3. Emit a review-set handoff with optional read-only index provenance validation.
- [x] 4. Resolve optional evidence-docs chunks through retained IDs and exact retrieval parameters.
- [x] 5. Preserve anonymous isolation and add mismatch, determinism, and wrapper regression coverage.
- [x] 6. Update the rule catalog, operator documentation, and validation runbook.
- [x] 7. Run focused and full validation, update implementation state, and open a PR to `dev`.
