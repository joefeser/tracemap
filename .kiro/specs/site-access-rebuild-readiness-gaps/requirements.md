# Site Access Rebuild-Readiness Gaps Requirements

## Goal

Publish issue #618 as a public-safe article that presents Access rebuild
readiness as a deterministic evidence-and-gap register rather than a promise.

## Requirements

1. Publish `/blog/access-rebuild-readiness-gaps/` through the article layout.
2. Explain query shapes, copy candidates, bindings, events, flows, bounds, and local review packaging.
3. Preserve rule IDs, tiers, provenance, coverage, truncation, omitted counts, supporting facts, limitations, and owner questions.
4. Cross-link both existing Access articles and the evidence, gap, manager, static/runtime, capability, change-review, and limitation pages.
5. Keep the claim level at `demo` and enforce issue #618's private-data and non-claim boundary.
6. Add focused route, sitemap, discovery, link, metadata, claim, and private-material validation.
