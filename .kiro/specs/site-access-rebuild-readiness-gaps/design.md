# Site Access Rebuild-Readiness Gaps Design

The article is generated from the existing blog registry and article shell. It
adds no public artifact packet and exposes no protected Access material. The
two earlier Access articles link to the gap-register route, and the new route
links back to both, creating an acquisition → lineage → readiness reading path.

A focused validator checks the generated route, canonical and sitemap entries,
discovery contract, reciprocal links, required rule/evidence vocabulary, and
the public claim/private-material boundary.
