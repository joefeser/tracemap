# Combined Dependency Diff Implementation State

Status: implemented

## Shipped Scope

- Added `tracemap diff` for deterministic combined-index source, coverage, endpoint, surface, edge, and opt-in path diffs.
- Implements source identity checks, reduced-coverage downgrades, stable keys, rule-backed gaps, Markdown/JSON output, selectors, caps, and `--exit-code`.

## Follow-Ups

- New diff dimensions should be introduced through a focused spec.
- Historical task subtasks are marked complete to prevent agents from treating shipped implementation details as active work.
