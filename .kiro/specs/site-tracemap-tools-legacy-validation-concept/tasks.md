# Tasks

- [x] 1. Add the legacy validation concept spec and implementation state.
  - Include `Public claim level: concept`.
  - Record that core legacy validation remains hidden until redacted summaries exist.

- [x] 2. Add the public concept page.
  - Publish `/legacy-validation/`.
  - Explain the validation plan, dimensions, safety model, and claim boundaries.
  - Link to the legacy validation spec source.

- [x] 3. Add discovery links and metadata.
  - Link from `/use-cases/`, `/demo/result/`, `/capabilities/`, `/validation/`, and `/packets/`.
  - Add `/legacy-validation/` to `site/src/_site/pages.json`.

- [x] 4. Validate.
  - Run `npm test`.
  - Run `npm run validate`.
  - Run `git diff --check`.
  - Run desktop and mobile browser sanity checks.

