# Cross-App Endpoint Alignment Implementation State

Status: implemented MVP with post-MVP backlog

## Shipped Scope

- Added `tracemap endpoints` for one client index and one server index.
- Supports Angular `HttpClient` evidence, ASP.NET route evidence, endpoint normalization, coverage labels, deterministic Markdown/JSON, and rule-backed endpoint findings.

## Post-MVP Backlog

The remaining endpoint families, N-to-N matching, runtime deployment concerns, proxy/auth behavior, and richer request/response type extraction are backlog items. They are plain bullets in `tasks.md`, not active unchecked tasks.
