# Site Graphify Lessons Requirements

- Publish `/blog/what-tracemap-learned-from-graphify/` as a bounded concept-level external-research story.
- Distinguish external observations, independently reproduced failure classes, implemented TraceMap behavior, deliberate differences, planned work, and non-adopted directions.
- Explain persistent graph state, relationship trust and sites, compiler-backed C# identity, incremental regression fixtures, reverse impact, independent implementation, licensing/attribution, and TraceMap's deterministic non-LLM boundary.
- Add registry, discovery, sitemap, external/source link, claim, private-material, and malformed-output validation.

