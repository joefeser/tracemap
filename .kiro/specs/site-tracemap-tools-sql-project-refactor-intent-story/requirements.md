# Site SQL Project Refactor Intent Story Requirements

## Goal

Publish issue #543 as a public-safe demo story about checked-in SQL project
refactor intent without strengthening static evidence into build, deployment,
database-state, compatibility, approval, or execution-safety claims.

## Requirements

1. Publish `/blog/sql-project-refactor-intent-evidence/` with the title
   “What a database rename can prove before deployment.”
2. Publish `/sql/project-refactor-intent/` as the compact proof surface.
3. Publish a deterministic synthetic JSON asset derived from
   `samples/sql-project-refactor` at commit
   `26189d1dd8d97d5741005ae7cbd033840099f216`.
4. Preserve rule IDs, evidence tiers, repository-relative spans, commit SHA,
   extractor family/version, coverage labels, safe identities, gaps, and
   limitations.
5. Attribute and link Microsoft’s July 28, 2026 article without implying
   affiliation, endorsement, or joint work.
6. Show table rename, column rename, and table schema-move categories while
   distinguishing fixture facts from illustrative supported categories.
7. Keep Tier 4 unsupported, ambiguous, unsafe, missing, and truncated gaps
   visible as reduced or partial coverage.
8. Link bidirectionally through database design review, SQL operator handoff,
   outputs, evidence, limitations, capabilities, the article, and the proof
   page without adding primary-navigation clutter.
9. Add sitemap/discovery/page metadata and focused claim, leak, provenance,
   link, and schema validation.
10. Keep the public claim level at `demo` and preserve every non-claim in issue
    #543.
