import assert from "node:assert/strict";
import { cp, mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { dirname, join, resolve } from "node:path";
import { fileURLToPath } from "node:url";
import test from "node:test";
import { buildSite } from "./build.mjs";
import { gapBecomesRuleRoute, validateGapBecomesRuleDist } from "./gap-becomes-rule.mjs";

const siteRoot = resolve(dirname(fileURLToPath(import.meta.url)), "..");
test("gap-to-rule article builds with concept-level discovery", async (t) => { const root = await fixture(t); await buildSite({ root, log() {} }); const errors = []; await validateGapBecomesRuleDist({ dist: join(root, "dist"), errors }); assert.deepEqual(errors, []); });
test("gap-to-rule validator rejects unsupported claims", async (t) => { for (const claim of ["TraceMap automatically fixes every gap.", "The validation is passed.", "The coverage is complete."]) await t.test(claim, async (subtest) => { const root = await fixture(subtest); await append(root, claim); await buildSite({ root, log() {} }); const errors = []; await validateGapBecomesRuleDist({ dist: join(root, "dist"), errors }); assert.match(errors.join("\n"), /unsupported positive claim/); }); });
test("gap-to-rule validator rejects private and executable material", async (t) => { for (const leak of ["/Us" + "ers/example/workspace", "/Us&#101rs/example/workspace", "Password=secret-value", "SELECT value FROM private_table"]) await t.test(leak, async (subtest) => { const root = await fixture(subtest); await append(root, leak); await buildSite({ root, log() {} }); const errors = []; await validateGapBecomesRuleDist({ dist: join(root, "dist"), errors }); assert.match(errors.join("\n"), /hard private material|source or executable material/); }); });
test("gap-to-rule validator scans card and generated discovery", async (t) => { const root = await fixture(t); const articlesPath = join(root, "src/_blog/articles.json"); const articles = JSON.parse(await readFile(articlesPath, "utf8")); articles.find((article) => article.slug === "how-a-gap-becomes-a-rule").cardDescription = "TraceMap guarantees complete coverage."; await writeFile(articlesPath, `${JSON.stringify(articles, null, 2)}\n`, "utf8"); await buildSite({ root, log() {} }); const discoveryPath = join(root, "dist/routes-index.json"); const discovery = JSON.parse(await readFile(discoveryPath, "utf8")); discovery.entries.find((entry) => entry.path === gapBecomesRuleRoute).summary = "/Us" + "ers/example/workspace"; await writeFile(discoveryPath, `${JSON.stringify(discovery, null, 2)}\n`, "utf8"); const errors = []; await validateGapBecomesRuleDist({ dist: join(root, "dist"), errors }); assert.match(errors.join("\n"), /blog card contains unsupported positive claim/); assert.match(errors.join("\n"), /discovery entry contains hard private material/); });
test("gap-to-rule validator reports sitemap, link, and claim-level drift", async (t) => { const root = await fixture(t); await buildSite({ root, log() {} }); await rm(join(root, "dist/sitemap.xml")); const pagePath = join(root, "dist/blog/how-a-gap-becomes-a-rule/index.html"); await writeFile(pagePath, (await readFile(pagePath, "utf8")).replaceAll("/proof-paths/", "/"), "utf8"); const discoveryPath = join(root, "dist/routes-index.json"); const discovery = JSON.parse(await readFile(discoveryPath, "utf8")); discovery.entries.find((entry) => entry.path === gapBecomesRuleRoute).publicClaimLevel = "demo"; await writeFile(discoveryPath, `${JSON.stringify(discovery, null, 2)}\n`, "utf8"); const errors = []; await validateGapBecomesRuleDist({ dist: join(root, "dist"), errors }); assert.match(errors.join("\n"), /sitemap is missing/); assert.match(errors.join("\n"), /missing required link/); assert.match(errors.join("\n"), /claim level must be concept/); });
test("gap-to-rule validator reports malformed discovery", async (t) => { for (const malformed of ["{", JSON.stringify({ entries: {} })]) await t.test(malformed, async (subtest) => { const root = await fixture(subtest); await buildSite({ root, log() {} }); await writeFile(join(root, "dist/routes-index.json"), malformed, "utf8"); const errors = []; await validateGapBecomesRuleDist({ dist: join(root, "dist"), errors }); assert.match(errors.join("\n"), /valid JSON|entries array/); }); });
async function fixture(t) { const root = await mkdtemp(join(tmpdir(), "tracemap-gap-rule-site-")); t.after(() => rm(root, { recursive: true, force: true })); await cp(join(siteRoot, "src"), join(root, "src"), { recursive: true }); return root; }
async function append(root, text) { const path = join(root, "src/_blog/articles/how-a-gap-becomes-a-rule.html"); await writeFile(path, `${await readFile(path, "utf8")}<p>${text}</p>`, "utf8"); }
